// const requireLogin = require("../middlewares/requireLogin");

module.exports = app => {
  // app.post("/savePrefs", requireLogin, async (req, res) => {
  //   const user = await req.user.save();
  //   res.send(user);
  // });

}